# /implement

Phase 4 · Execute tasks; one isolated worker + one commit per task (TDD).

Follow the canonical procedure in `flow/implement.md` and obey
`memory/constitution.md`. Produce the phase's artifact, then stop at the human
gate — do not auto-advance.
