# /sync

Phase R · Reverse path: detect & reconcile drift between spec and shipped code.

Follow the canonical procedure in `flow/sync.md` and obey
`memory/constitution.md`. Produce the phase's artifact, then stop at the human
gate — do not auto-advance.
